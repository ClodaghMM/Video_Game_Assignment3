using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PacStudentController : MonoBehaviour
{

    //Implement movement in the following way
    //linear lerping to move from one grid position to another. //lerp at fixed speed and be frame-rate independent (time.fixedDeltaTime)



    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
